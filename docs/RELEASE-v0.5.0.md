# SIRA v0.5.0

- Dashboard Standard remains the default page at `/`.
- Admin Dashboard at `/admin` now restores the platform overview from earlier releases.
- The top-right Admin Dashboard icon opens only the platform/system overview.
- Platform release information now uses the current application version dynamically.
